{
  "name": "user-research-tracking-tool-for-behavioral-analysis",
  "version": "0.0.6",
  "description": "API written on Javascript, for tracking the logs during a usability test session. By completing each use case, the script generates a file that is a detailed list of all user's interactions. This list can then be consumed in order to visualise the data and create comparable graphics.",
  "main": "api/init.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.wdf.sap.corp:ux-design-tnt/user-research-tracking-tool-for-behavioral-analysis.git"
  },
  "keywords": [
    "ux",
    "user",
    "research"
  ],
  "author": "",
  "license": "",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-jshint": "^1.1.0",
    "grunt-contrib-nodeunit": "~1.0.0",
    "grunt-contrib-uglify": "~2.3.0",
    "grunt-contrib-watch": "^1.0.0"
  }
}
